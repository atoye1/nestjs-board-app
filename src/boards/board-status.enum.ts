export enum BoardStatus {
  PUBLIC = 'PUBLIC',
  PRIVATE = 'PRIVATE',
}

// Entitiy에 정의되어 있으므로 이제 필요없어짐
// export interface Board {
//   id: string;
//   title: string;
//   description: string;
//   status: BoardStatus;
// }
